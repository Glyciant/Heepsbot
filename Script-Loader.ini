alias scripts {
  if ($1 = load) {
    load -rsN scripts\Remote\ApiLookup.ini
    load -rsN scripts\Remote\AutoWelcome.ini
    load -rsN scripts\Remote\Betting.ini
    load -rsN scripts\Remote\Commands.ini
    load -rsN scripts\Remote\Core.ini
    load -rsN scripts\Remote\Countdown.ini
    load -rsN scripts\Remote\Echo.ini
    load -rsN scripts\Remote\Emote.ini
    load -rsN scripts\Remote\Follow.ini
    load -rsN scripts\Remote\Games.ini
    load -rsN scripts\Remote\Hosting.ini
    load -rsN scripts\Remote\Levels.ini
    load -rsN scripts\Remote\Log.ini
    load -rsN scripts\Remote\Moderation.ini
    load -rsN scripts\Remote\OnlineCheck.ini
    load -rsN scripts\Remote\Points.ini
    load -rsN scripts\Remote\Poll_Raffle.ini
    load -rsN scripts\Remote\ProtectActions.ini
    load -rsN scripts\Remote\ProtectASCII.ini
    load -rsN scripts\Remote\ProtectBlacklist.ini
    load -rsN scripts\Remote\ProtectCaps.ini
    load -rsN scripts\Remote\ProtectEmotes.ini
    load -rsN scripts\Remote\ProtectLinks.ini
    load -rsN scripts\Remote\ProtectLones.ini
    load -rsN scripts\Remote\ProtectParagraph.ini
    load -rsN scripts\Remote\ProtectRepeat.ini
    load -rsN scripts\Remote\ProtectSymbols.ini
    load -rsN scripts\Remote\ProtectUsernames.ini
    load -rsN scripts\Remote\Quotes.ini
    load -rsN scripts\Remote\Record.ini
    load -rsN scripts\Remote\SubWelcome.ini
    load -rsN scripts\Remote\Tweet.ini
    load -rsN scripts\Remote\Uptime.ini
    load -rsN scripts\Remote\UserFeatures.ini
  }
  elseif ($1 = unload) {
    unload -rsN scripts\Remote\ApiLookup.ini
    unload -rsN scripts\Remote\AutoWelcome.ini
    unload -rsN scripts\Remote\Betting.ini
    unload -rsN scripts\Remote\Commands.ini
    unload -rsN scripts\Remote\Core.ini
    unload -rsN scripts\Remote\Countdown.ini
    unload -rsN scripts\Remote\Echo.ini
    unload -rsN scripts\Remote\Emote.ini
    unload -rsN scripts\Remote\Follow.ini
    unload -rsN scripts\Remote\Games.ini
    unload -rsN scripts\Remote\Hosting.ini
    unload -rsN scripts\Remote\Levels.ini
    unload -rsN scripts\Remote\Log.ini
    unload -rsN scripts\Remote\Moderation.ini
    unload -rsN scripts\Remote\OnlineCheck.ini
    unload -rsN scripts\Remote\Points.ini
    unload -rsN scripts\Remote\Poll_Raffle.ini
    unload -rsN scripts\Remote\ProtectActions.ini
    unload -rsN scripts\Remote\ProtectASCII.ini
    unload -rsN scripts\Remote\ProtectBlacklist.ini
    unload -rsN scripts\Remote\ProtectCaps.ini
    unload -rsN scripts\Remote\ProtectEmotes.ini
    unload -rsN scripts\Remote\ProtectLinks.ini
    unload -rsN scripts\Remote\ProtectLones.ini
    unload -rsN scripts\Remote\ProtectParagraph.ini
    unload -rsN scripts\Remote\ProtectRepeat.ini
    unload -rsN scripts\Remote\ProtectSymbols.ini
    unload -rsN scripts\Remote\ProtectUsernames.ini
    unload -rsN scripts\Remote\Quotes.ini
    unload -rsN scripts\Remote\Record.ini
    unload -rsN scripts\Remote\SubWelcome.ini
    unload -rsN scripts\Remote\Tweet.ini
    unload -rsN scripts\Remote\Uptime.ini
    unload -rsN scripts\Remote\UserFeatures.ini
  }
  else {
    echo -a Usage /scripts (load|unload)
  }
}
